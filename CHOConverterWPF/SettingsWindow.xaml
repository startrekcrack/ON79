    <Window x:Class="CHOConverterWPF.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="200" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Icon="Assets/ChoConverter.png"
        Background="{DynamicResource WindowBackgroundBrush}"
        SourceInitialized="Window_SourceInitialized">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="lblLanguage" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" />
        <ComboBox x:Name="cmbLanguage" Grid.Row="0" Grid.Column="1" MinWidth="160" />

        <TextBlock x:Name="lblTheme" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,8,10,0" />
        <ComboBox x:Name="cmbTheme" Grid.Row="1" Grid.Column="1" MinWidth="160" Margin="0,8,0,0" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="btnCancel" Width="90" Margin="0,0,10,0" Click="btnCancel_Click" />
            <Button x:Name="btnOk" Width="90" Click="btnOk_Click" />
        </StackPanel>
    </Grid>
</Window>
